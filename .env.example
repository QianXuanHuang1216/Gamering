# 示例（无真值）。真值只放本地 .env，严禁进 git。
DISCORD_CLIENT_ID=
DISCORD_CLIENT_SECRET=
DISCORD_BOT_TOKEN=
DISCORD_PUBLIC_KEY=
SESSION_SECRET=本地随机串即可
SITE_URL=http://localhost:3000
# DB 本地文件：~/persistent/gamering/data/gamering.db（WAL，不进 git）
# DB_PATH=~/persistent/gamering/data/gamering.db（不设则 :memory:，重启丢数据）
# DATABASE_URL= postgres 切换时再填，接口保持一致
